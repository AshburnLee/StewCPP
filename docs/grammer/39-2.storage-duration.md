在标准里的含义是：对象所占的那块存储，从什么时候开始有效、到什么时候结束。描述的是“存储/对象生命期模型”。

在此基础上选择自动栈、静态全局、new/智能指针等，就是是常说的管理策略。


## 存储期类型

- 自动存储期（automatic storage duration）：它的生命周期仅限于函数调用的范围内。自动构造和析构+栈分配+无需手动管理+生命周期限制
- 静态存储期（static storage duration）
- 线程局部存储期（thread-local storage duration）
- 动态存储期（dynamic storage duration）

存储期回答的是：这个变量从什么时候开始占着内存、到什么时候必须结束，谁负责申请、谁负责释放。

- 自动存储期：就是函数里的普通局部变量（以及函数参数）。一进*函数*就准备好这块内存，一旦 return 就没了；通常压在*栈*上，不用写 `delete`。适合：函数里算一算、用完就扔的中间量。*从进入函数=>函数结束*

- 静态存储期：全局变量、命名空间变量、以及带 `static` 的*局部变量*。*程序*启动时就有地方放它们，进程结束才释放；`static` 局部变量只初始化一次，下次进函数还是上次留下的值。适合：要跨很多次函数调用还记着的计数器、配置开关之类。*从程序开始=>程序结束*。

- 线程局部存储期：变量用 `thread_local`（或 C 里线程局部扩展）声明。每个线程各自有一份，A 线程改自己的，B 线程看不到。线程*launch时*按线程建，线程*结束时*跟着没。适合：多线程里每个线程自己的“当前上下文”（例如当前请求 id），又不用到处传参。*从线程开始=>线程结束*。

- 动态存储期：你用 `new` / `malloc` 向堆要来的那块内存，直到`delete` / `free`（或智能指针帮你释放）才算结束。适合：编译时不知道要多少个元素（运行时用户输入 `n` 再分配）、或寿命要和函数调用对不齐（函数返回后其他地方处还要用）的数据。*从 `new` / `malloc` =>`delete` / `free`*


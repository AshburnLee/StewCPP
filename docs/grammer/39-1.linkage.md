
这种*对外*和*对内*表达的组合其实是*封装*的一种体现。把不必要暴露的不暴露。这和类里用 private 藏成员是同一类思想：信息隐藏 + 缩小依赖面；差别只是 C++ 里以“翻译单元 / 链接” 为边界，而*不只*靠 class 关键字。


## Linkage [链接：名字能不能跨 .cpp「对上号」]

链接性说的是：这个名字在链接阶段会不会被当成“整个程序里唯一的那一份”，还是“每个 .cpp 各用各的”，还是根本不参与*跨文件*对符号。

编译器会把带链接的名字写进目标文件（`.o`）的符号表（带 `static`/内部、或 `global` 等属性）；*链接器*再按这些属性决定：多个 `.o` 里同名是要合并成一处，还是允许重名各算各的。

1. 无链接性（No linkage）

- 这个名字只在你写的那段 `{ }` 里有意义，链接器不管它和别的文件里有没有同名。
- 常见：函数体内的局部变量、函数的参数名、块里的 `enum`、块里的 `typedef` 等（按规则属于无链接的名字）。
- 编译器/链接器行为：一般不会为「普通局部变量」生成「给链接器用来合并」的全局符号；别的 `.cpp` 也不可能通过名字去引用它。


2. 内部链接性（Internal linkage）

- `a.cpp` 里叫 `foo`，`b.cpp` 里也可以叫 `foo`，互不相干，链接器区别对待。
- 常见：文件作用域的 `static` 全局变量/函数；namespace{} 里的名字（C++ 里推荐用 naemspace{} 代替文件作用域 `static`）。
- 编译器/链接器行为：编译器在符号表里把这个符号标成仅本翻译单元可见（实现上常带 `static` 或本地符号）；链接时允许多个 `.o` 里出现同名，各保留一份，不会报「重复定义」成同一个实体的那种冲突。


3. 外部链接性（External linkage）

- 整个程序里，同一个函数/全局变量应该*只有一处*定义，别的 `.cpp` 只能通过声明去用同一块内存、同一段代码。
- 常见：普通`void f();`（不在类里、不在函数里）、非 `static` 且非 `const` 的全局变量（以及带 `extern` 的变量声明等，按语境区分声明/定义）。
- 编译器/链接器行为：编译器在 `.o` 里留下外部符号；链接器在所有 `.o`/库里找同一个名字，解析成唯一地址。若出现两个 .cpp 都定义了同名外部函数/全局变量，通常会 链接错误：multiple definition（`inline`/`constexpr` 函数、模板、`const int` 等例外按标准另说）。

`extern int x;`（无初始化）多半是声明：告诉编译器「别处有定义」，符号仍按外部链接参与解析；不要和“函数前向声明”混为一谈，*函数本来就可以只声明、在别文件定义*；`extern` 主要用于*变量*在头文件里声明、在某一 `.cpp` 里定义这一种写法。

---

函数 不在类里、不在函数里 在.cpp 最外层，即文件内全局函数/变量

1. 前面加了 static 的文件内全局函数/变量

- 这个名字*只给*这个 .cpp 用，别的 .cpp 看不见、也链不上。	
- 对于链接器而言，每个 .cpp 里可以*各自*有一个同名函数，互不冲突。	
- 匿名 `namespace{}` 也表示*只给*本文件使用，C++ 常用 ，*最佳实践是用 `namespace {}` 代替这个语境下的 static*。

2. 没 static 的文件内全局函数/变量

- 这个名字全程序*共用*一份，别的 .cpp 只要声明一下就能调用；整个程序里只能有一处定义。
- 所有 .cpp 里同名只能合并成同一个，重复定义会报错。


实例：

~~~cpp
static 全局常量：只在本翻译单元用，不占全局符号名
// loader 里 KiB/MiB/GiB，避免魔数、且不必导出给其他 .cpp
static const std::size_t K = 1024;
static const std::size_t M = K * K;

// 外部连接性的，但是使用了支队本文件可见的M, 是常见写法. 
// bytes_to_mib 有外部链接，只表示：别的 .cpp 可以声明 extern 后调用它；
// 真正带实现的定义只有这一份，链接时和本文件里的 M 绑在一起。
// 
// 链接器不会要求「外部函数只能用外部变量」之类的事；
// 别的也不会单元「误用 M」——他们根本链不到 M。
//
//  M 不会作为"整个程序里大家都能来连一下"的那种对外符号暴露出去
// 别人没有链接到 M 的这条路
// 外面能链到的只有 bytes_to_mib 这个函数（一个符号、一处实现）,
// 编译本文件时，编译器在生成 bytes_to_mib 的机器码里仅仅是用到了 M 的值，M 仍然没有外部链接
std::size_t bytes_to_mib(std::size_t bytes) { return bytes / M; }
~~~




特化就是为**特定的类型**提供**特定的实现**，“部分约束，不分可保留”

# 模板全特化与偏特化

什么是**主模版**？第一次写下来的那份 “最通用的模板定义”，没有对参数做任何“形态约束” 或具体替换。之后的全特化和偏特化都以 主模版为根。

- 模板全特化：为特定类型提供特殊的实现，适用于函数模板和类模板。
- 模板偏特化：**仅适用于类模板**，允许为部分模板参数提供特殊实现。


## 全特化

全特化是指为模板的**所有参数**提供具体类型。`template<>` 标记“这是特化”

~~~cpp
// 函数模板
template<typename T>
void print(T value) {
    std::cout << "Generic: " << value << std::endl;
}

// 特化版本，不需要占位符T
template<> 
void print(const char* value) {
    std::cout << "I am Specialized: " << value << std::endl;
}
~~~

## 偏特化

- 允许在模板的**某些参数已经确定**的情况下，为剩余参数提供特殊实现。
- 用于类模板，而不适用于函数模板。
- 偏特化必须保持与原模板相同的参数数量。

~~~cpp
template<class T, class D>
class U;              // 主模板

template<class T, class D>
class U<T[], D>;      // 偏特化 形态特化
~~~

`T[]` 是偏特化， `U<T[], D>` 不是把所有参数定死，而是规定了“第一个参数形态必须是数组 []（约束），元素类型仍可变（T）”。这体现了“部分约束，不分可保留”的偏特化特性


## 函数模版为什么没有偏特化？

C++ 语法不允许。请使用函数重载实现区分。
~~~cpp
// 通用版本
template <typename T>
void print(T v) {
    cout << "generic: " << v << "\n";
}
// 指针版本（用重载表达“部分匹配”）
template <typename T>
void print(T* p) {
    if (p) cout << "pointer: " << *p << "\n";
    else   cout << "pointer: null\n";
}
~~~
